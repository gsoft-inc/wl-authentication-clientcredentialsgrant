openapi: 3.0.1
info:
  title: Test API
  version: v1
paths:
  /example:
    get:
      tags:
        - ClientCredentials
      parameters:
        - name: id
          in: query
          style: form
          schema:
            type: integer
            format: int32
      responses:
        '200':
          description: Success
components: { }