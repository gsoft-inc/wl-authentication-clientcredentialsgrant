openapi: 3.0.1
info:
  title: Test API
  version: v1
paths:
  /example:
    get:
      tags:
        - TypedResultNoProduces
      operationId: GetExample2
      parameters:
        - name: id
          in: query
          style: form
          schema:
            type: integer
            format: int32
      responses:
        '200':
          description: '200'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TypedResultExample'
        '400':
          description: '400'
          content:
            application/json:
              schema:
                type: string
        '404':
          description: '404'
components:
  schemas:
    TypedResultExample:
      required:
        - count
        - name
      type: object
      properties:
        name:
          type: string
        count:
          type: integer
          format: int32
        description:
          type: string
          nullable: true
      additionalProperties: false